import css from './passkey-sign-in-panel.module.scss';

type Props = Record<string, never>;

export function PasskeySignInPanel(_props: Props) {
    return (
        <div class={css.root}>
        </div>
    );
}
