{{#if includeCss}}import css from './{{componentFileName}}.module.scss';

{{/if}}{{#if includeType}}type Props = object;

{{/if}}export function {{componentName}}({{#if includeType}}_props: Props{{/if}}) {
    return (
{{#if includeCss}}        <div class={css.root}>
			// ...
        </div>
{{else}}        <div>
			// ...
        </div>
{{/if}}    );
}
