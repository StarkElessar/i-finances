# Migration map

| Legacy source | Target module | Status | Notes |
| --- | --- | --- | --- |
| `src/server/db` | `apps/api/src/infrastructure/database` | scaffolded | Moved mechanically; SQL and schema content unchanged. |
| `drizzle` | `apps/api/drizzle` | scaffolded | Existing migrations, snapshots, and journal preserved. |
| `src/server/category` | `apps/api/src/modules/category` + `apps/api/src/http/category-controller.ts` | implemented | Classes for repository, rules, application service, and Hono controller; public categories contract preserved. |
| `src/entities/category/api` | `packages/contracts` + `apps/web/src/features/categories` | implemented | Contracts, HTTP client, CRUD editor/list, archive/restore, budgets, descriptions, keywords, and optimistic locking migrated. |
| `src/views/sign-in/api` | `packages/contracts` + `apps/web/src/features/auth` | partial | Password and passkey sign-in/session client and UI migrated; legacy visual parity remains. |
| `src/server/account` | `apps/api/src/modules/account` + `apps/api/src/http/account-controller.ts` | implemented | Household-scoped CRUD, optimistic locking, archive/restore, and explicit currency correction migrated; correction uses an internal exchange-rate adapter and does not change migrations. |
| `src/server/contact` | `apps/api/src/modules/contact` + `apps/api/src/http/contact-controller.ts` | implemented | Household-scoped normalization, duplicate-name protection, CRUD, archive/restore, optimistic locking, and web CRUD feature migrated. |
| `src/server/operation` | `apps/api/src/modules/operation` + `apps/api/src/http/operation-controller.ts` + `apps/web/src/features/operations` | implemented | Household-scoped CRUD, soft archive/restore, ledger/balance/monthly aggregates, historical rate snapshots, explicit rate recalculation, optimistic locking, and an adaptive ledger table migrated. |
| `src/server/auth` | `apps/api/src/modules/auth` + `apps/api/src/http/auth-controller.ts` + `apps/api/src/http/passkey-controller.ts` | implemented | Password and WebAuthn ceremonies, Argon2 verification, rate limiting, strict origin guard, cookie sessions, replay-protected credentials, and revoke flow migrated. |
| `src/server/receipt-import` | `apps/api/src/modules/receipt-import` + `apps/api/src/http/receipt-import-controller.ts` + `apps/api/src/http/receipt-worker-controller.ts` + `apps/web/src/features/receipt-import` | implemented | Upload, private image storage, category snapshot, worker lease/heartbeat/complete/fail, editable review draft with optimistic locking, revision, explicit approve, and linked operation creation migrated; real OCR worker remains external. |
| `src/views`, `src/widgets`, `src/shared/ui` | `apps/web/src/pages` + `apps/web/src/widgets/app-shell` + `apps/web/src/shared/ui` | partial | Client routing, protected app-shell, separate home/categories/contacts/receipts pages, category presentation, and operation table are migrated; account/contact/receipt visual parity and the remaining global feature styles are still in progress. |
