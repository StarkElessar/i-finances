> **Статус: дерево решений закрыто, план написан.** См.
> `docs/superpowers/plans/2026-09-17-operations-view-mode.md`.

# Два режима отображения операций: черновик решений (grill-сессия)

Задача из бэклога: «Два режима отображения операций: таблица (десктоп/планшет)
и упрощённый список (мобилка по умолчанию), с ручным переключением
пользователем».

Как пользоваться этим файлом: под каждым вопросом впишите свой ответ прямо
под строкой `Ответ:` (согласны с рекомендацией — просто напишите "да" или
оставьте пустым, тогда беру рекомендацию по умолчанию). В следующей сессии
попросите меня прочитать этот файл — продолжим с того же места.

## Раунд 1

### Q1 — Масштаб задачи
Таблица операций используется только в `home/page.tsx`. Делаем dual-mode
только для неё, или это должен быть переиспользуемый паттерн?

Рекомендация: только этот экран, без преждевременной абстракции.

Ответ:

---

### Q2 — Граница breakpoint'а
Из формулировки задачи планшет должен получать таблицу → нужен breakpoint
768px (существующий CSS-токен), а не 961px (используется только для панели
деталей операции и не подходит по смыслу).

Рекомендация: 768px (`mx.media-mn(768)` / существующий токен).

Ответ:

---

### Q3 — Где хранить ручной выбор пользователя
В приложении сейчас нет инфраструктуры пользовательских настроек вообще.
Варианты: (1) localStorage на устройстве, (2) серверная настройка
(household/пользователь, требует миграции+API), (3) не сохранять вовсе.

Рекомендация: localStorage (вариант 1).

Ответ:

---

### Q4 — Семантика переключения
Тумблер — это постоянный трёхпозиционный `auto | table | list`, который
сохраняется в localStorage и переживает любые перезагрузки/breakpoint'ы, пока
пользователь сам не вернёт `auto`?

Рекомендация: да, именно так.

Ответ:

---

### Q5 — Общий тулбар vs раздельный
Сейчас поиск, направление сортировки и период (неделя/месяц/год) зашиты
внутри `OperationsTable` вместе с самой таблицей. Предлагается вынести их в
общую обёртку, а `<Grid>`/`<OperationsList>` рендерить как сменное тело.

Рекомендация: да, общий тулбар, общее тело меняется по режиму.

Ответ:

---

### Q6 — Состав карточки в списке
Таблица показывает 7 колонок (Дата, Название, Сумма, Баланс счёта, Категория,
Контакт, Комментарий). Предложение для карточки на мобилке:
- строка 1: иконка категории (цвет) · Название (обрезается) · Сумма
- строка 2 (мельче): Дата · Баланс счёта
- Контакт и Комментарий — не показывать инлайн, доступны по тапу в панели
  деталей операции (она и так открывается по клику).

Рекомендация: согласиться с этим составом.

Ответ:

---

### Q7 — Сортировка/группировка в списке
Таблица допускает сортировку по 5 полям (дата/сумма/баланс/категория/контакт),
группировка идёт вслед за сортировкой. Сохраняем весь функционал в списке
(тот же переключатель сортировки из общего тулбара, см. Q5), просто без
resizable-колонок, или список — намеренно упрощённый режим с фиксированной
сортировкой "по дате" для v1?

Рекомендация: сохранить сортировку — переключатель уже общий элемент тулбара,
дополнительных усилий не требует.

Ответ:

---

## Дальше

После вашего раунда ответов будет ещё один-два раунда вопросов (состав следующих
веток дерева решений зависит от ответов выше — например, если Q3/Q4 поменяются,
изменится и то, что спрошу про реализацию хранения). Когда дерево решений будет
закрыто — соберу это в нормальный implementation-план.
